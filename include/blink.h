#ifndef BLINK_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define BLINK_H

#include <Arduino.h>

void blink(uint8_t pin, int ms);
#endif
